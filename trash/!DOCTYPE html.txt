<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Laravel + Vue SPA</title>

    @if(app()->environment('local'))
        {{-- Modo desenvolvimento: usa Vite dev server --}}
        @vite(['resources/js/main.js', 'resources/js/style.css'])
    @else
        {{-- Modo produção/build: usa arquivos compilados --}}
        @php
            $manifest = json_decode(file_get_contents(public_path('build/manifest.json')), true);
        @endphp
        <link rel="stylesheet" href="{{ asset('build/' . $manifest['resources/js/style.css']['file']) }}">
        <script type="module" src="{{ asset('build/' . $manifest['resources/js/main.js']['file']) }}"></script>
    @endif
</head>
<body>
    <div id="app"></div>
</body>
</html>